{% extends 'base.html.twig' %}

{% block title %}Paiement réussi{% endblock %}

{% block body %}
    <div class="container text-center mt-5">
        <h1 class="text-success">{{ message }}</h1>
        <a href="{{ path('app_home') }}" class="btn btn-outline-success mt-4">
            Retour à l'accueil
        </a>
    </div>
{% endblock %}
